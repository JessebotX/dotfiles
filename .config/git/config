# Git's per user config thing
[core]
    editor = gvim -v
[user]
    email = jessebot.noreply@protonmail.com
    name = Jesse
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
