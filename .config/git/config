# Git's per user config thing
[core]
	editor = vim
[user]
	email = username.git@posteo.net
	name = iii...iii
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
